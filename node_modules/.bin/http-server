#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../http-server/bin/http-server" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../http-server/bin/http-server" "$@"
  ret=$?
fi
exit $ret
