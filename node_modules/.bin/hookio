#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../hook.io/bin/hookio" "$@"
  ret=$?
else 
  node  "$basedir/../hook.io/bin/hookio" "$@"
  ret=$?
fi
exit $ret
